[package]
name = "ds-guizhou"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
log = "0.4"
ciborium = "0.2"
arrow-schema = { version = "52.1", features = ["serde"] }
mems = { path = "../../../mems" }
csv = "1.3.0"
bytes = "1.6.1"
chrono = "0.4.38"

